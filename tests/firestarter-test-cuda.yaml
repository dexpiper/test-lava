metadata:
    format: Lava-Test Test Definition 1.0
    name: firestarter-test
    description: "Starts FIRESTARTER tests"
    maintainer:
        - a.kurilov@yadro.com

run:
    steps:
        - bash ./tests/run_approp_firestarter.sh -d -t 300 -l 50
